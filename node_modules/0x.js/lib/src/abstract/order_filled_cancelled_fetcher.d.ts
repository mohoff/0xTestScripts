import { BigNumber } from '@0xproject/utils';
export declare abstract class OrderFilledCancelledFetcher {
    abstract getFilledTakerAmountAsync(orderHash: string): Promise<BigNumber>;
    abstract getCancelledTakerAmountAsync(orderHash: string): Promise<BigNumber>;
}
